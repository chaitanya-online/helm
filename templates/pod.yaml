apiVersion: v1
kind: Pod
metadata:
  name: {{ .Values.pod.name }}
  labels:
    labels:
    name: apache
    tier: frontend
spec:
  containers:
    - name: {{ .Values.pod.name }}
      image: richeb/apachetest:{{ .Values.image.tag }}
      ports:
        - containerPort: 80